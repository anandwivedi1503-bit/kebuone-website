"use client";

import PartnerSpotlight from "../PartnerSpotlight/PartnerSpotlight";

type Props = {
  showApply?: boolean;
};

export default function DealerNetwork(_props: Props) {
  return <PartnerSpotlight />;
}
